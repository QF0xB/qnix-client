# Archived pre-SDK configuration. It remains available locally for reference,
# but is deliberately outside the new client configuration history.
old/

# Development and build artefacts
.direnv/
result
result-*
*.qcow2
*.qcow2.*
*.fd
.codegraph/
